apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example-paper
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/0x1malia/msud-paper-gitops-lab
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: katie-paper
  syncPolicy:
    automated:
      prune: true
      selfHeal: true